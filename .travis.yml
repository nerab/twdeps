language: ruby

rvm:
  - 2.4.2

before_script:
 - mkdir ~/.task
 - echo data.location=~/.task > ~/.taskrc

before_install:
 - sudo apt-get update
 - sudo apt-get install task
 - sudo apt-get install graphviz
